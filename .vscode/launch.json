{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Web: Debug Dev Server (4200)",
            "preLaunchTask": "web: start (npm.cmd)",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Web: Debug Built Preview (8080)",
            "preLaunchTask": "web: preview (npm.cmd)",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}